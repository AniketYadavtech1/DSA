product(){
  List <int> product = [1,2,3,4,5,6,7,8,9];

   List <String> productList = ["rambhvan","ramjanam", "pintu","sintu","papa"];
  // for(int i =0;i<product.length;i++){
  //   // print(product[i]);
  // }
  // for(int product=0;product<productList.length;product++){
  //   // print(productList[product]);
  // }
  // product.map((e)=>productList.length);
  // product.forEach((product)=>print(product* 2));
  // productList.map((e)=>productList.length).toList();


productList.map((e)=>productList.length).toList();
  print(productList);
  print(productList.length);
  print(product.length);
}




void main(){
  product();
}